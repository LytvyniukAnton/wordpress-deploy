- name: Create Docker volume for aaPanel
  community.docker.docker_volume:
    name: aapanel_data
    state: present

- name: Run aaPanel container
  community.docker.docker_container:
    name: aapanel
    image: btpanel/aapanel:latest
    state: started
    restart_policy: unless-stopped
    ports:
      - "8888:8888"
    volumes:
      - aapanel_data:/www